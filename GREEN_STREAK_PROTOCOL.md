# 🟩 THE GREEN STREAK PROTOCOL (Standing Order)

## 🎯 Objective
Maintain a **100% Daily GitHub Contribution Streak** on [Mamidi7](https://github.com/Mamidi7).
**Why:** Psychological warfare. Recruiters & Interviewers must see a "Disciplined, Obsessed GenAI Engineer."

## 📂 Repository Strategy
1.  **`gcp-genai-daily-grind` (PRIVATE):**
    *   **Purpose:** The "Daily Journal". Raw learning, experiments, notes, diagrams, small scripts.
    *   **Content:** Everything we do (WIF notes, Box Office script, RAG tests).
    *   **Frequency:** **DAILY.** No excuses.
    *   **Visibility:** Private (Enable "Private Contributions" in GitHub profile settings).

2.  **`legal-ai-agent` (PUBLIC - Showcase):**
    *   **Purpose:** The "Masterpiece". Production-grade code only.
    *   **Security:** **NO KEYS EVER.** Use `.env` and `gcloud secret manager`.
    *   **Frequency:** Only when a feature is "polished" (e.g., "Added CI/CD", "Refactored Dockerfile").
    *   **Backup:** Keep a local copy in `~/projects/Legal_AI_Agent_Secure_Backup` just in case.

## 📅 The "Unavailable" Protocol (Auto-Pilot)
If Boss (Krishna) is **not available** or **busy**:
1.  **Subba Rao's Duty:** I will generate a meaningful "contribution" file locally.
    *   *Examples:* "Updated System Design Diagram for Video Rec", "Refined RAG Retrieval Prompts", "Added Unit Test for API Wrapper".
2.  **Action:** I will create the file and tell you: *"Boss, I prepared `docs/rag_optimization_notes.md`. Run `git push` to keep the streak alive."* (Since I cannot push without your auth/confirmation usually, I prepare the bullet; you fire the gun).
    *   *Advanced:* If we set up a cron job + SSH keys, I could theoretically auto-push, but for now, **I prep, you push.**

## 🛡️ Security Protocol
-   **Pre-Commit Check:** ALWAYS scan for `API_KEY`, `sk-`, `AIza` before adding files.
-   **Secrets:** All keys go to `.env` (added to `.gitignore`).

## 📝 Today's Action (Feb 18, 2026)
1.  Initialize `gcp-genai-daily-grind`.
2.  Commit `scripts/box_office_generator.py` (The Python Automation).
3.  Commit `docs/wif_setup_guide.md` (The Cloud Security Study).
