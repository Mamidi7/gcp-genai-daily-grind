# Daily Log — 2026-05-30

## What We Covered

- **Coding Agent as a Deep Skill** — Realization that agent proficiency is a craft, not a vending machine. Vague prompts produce garbage; the skill ceiling is high. Documented as reusable insight for interview answers about AI tool workflows.
- **Snowflake DE Concepts** — Applied in quiz format: Result Cache (24h reuse, zero cost), Snowpipe vs Snowpipe Streaming (file vs row ingestion), ROW_NUMBER() with QUALIFY (dedup pattern), clustering key threshold (>1TB + selective filters).
- **Editorial Notebook Design Pattern** — Built reusable notebook-aesthetic HTML template with CSS variables, dark/light mode, sidebar navigation, A+/A- font controls, quiz system.
- **Hermes Disaster Recovery** — Full environment backup (27MB compressed) across 4 locations: laptop local, iCloud Drive, GCS bucket (gcp-ai-etl-hermes-backup), GitHub private repo (Mamidi7/hermes-backup). Encrypted secrets with AES-256.

## Code Built

- `docs/editorial-notebook-template.html` — Reusable editorial notebook HTML template with: CSS variable theming (light/dark), sticky topbar with A+/A- font controls (12-20px, localStorage-persisted), sidebar with search + nav + mobile hamburger, details/summary concept cards, quiz system with progress bar, telugu analogy block, callout components (success/warn/info/danger), responsive layout.
- `docs/snowflake-de-manual.html` — Snowflake Data Engineering reference manual (1505 lines). Built on editorial notebook template. Covers: Architecture (storage-compute separation, micro-partitions, virtual warehouses), Ingestion (COPY INTO, Snowpipe, Snowpipe Streaming, Streams+Tasks), SQL Patterns (MERGE with QUALIFY dedup, CTE, window functions, UDF), Data Modeling (star schema, SCD, clustering), Performance (result cache, warehouse sizing, clustering keys, materialized views). Includes 4-question interactive knowledge check quiz.

## Errors Debugged

- **Details/summary styling glitch** — The `<details><summary>` concept cards had awkward characters and missing hover states. Fixed by removing ::before pseudo-element for the triangle indicator, using clean flat style instead, adding background change on summary open with proper body spacing.
- **Quiz answer reveal not showing explanations** — Quiz cards weren't showing explanation text on answer click. Fixed by ensuring poll-reveal div structure and .show class toggle in the quiz JS handler.
- **Sidebar clutter** — Collapse/expand toggle buttons were confusing. Simplified by removing separate collapse/expand controls, keeping clean nav with mobile hamburger toggle only. Desktop sidebar stays fixed.

## Interview Prep

- **Snowflake concepts covered:**
  - 30s pitch: "Snowflake separates storage and compute completely — multiple virtual warehouses query the same data without contention, micro-partitions enable automatic pruning, result cache eliminates compute on repeated queries."
  - STAR framing delivered via 4 knowledge check questions covering real SC (Result Cache usage, Snowpipe architecture, QUALIFY dedup for CDC, clustering key strategy)
- **Coding agent skill insight:** Can be framed as a behavioral answer for "How do you work with AI tools?" — treat as craft, iterate on prompts, decompose tasks, review output critically.

## Tomorrow

- HTML skill workflow improvement — Krishna wants to go back to the old concept-explain approach with better animations, SVG diagrams, and templates from htmlrev.com
- Continue editorial notebook template refinement for reuse across study notebooks
- Next: explore htmlrev.com templates for design inspiration
