# Daily Log — 2026-05-21

## What We Covered

- **FDE 101 Guide** — Created and committed `docs/fde-101-guide.html`, a comprehensive Forward Deployed Engineering terminology mapping guide. Paper-aesthetic notebook covering FDE role, key terminology (P0/P1 triage, customer-facing engineering, production debugging), and interview prep context.

- **GCP Cost Cleanup** — Decided to stop all GCP compute resources to avoid idle billing. Deleted Cloud Composer (`imdb-composer`, ~$8-12/day saved) and Cloud Run (`flask-app1`). Preserved GCS buckets and BQ dataset `elt_raw` (storage only, pennies/month). Running cost now ~$0/day for compute.

- **IMDb Pipeline Architecture Decision** — Finalized the architecture direction: run data extraction locally on Mac (RapidAPI calls complete in 2s locally vs timeout on Composer workers), upload to GCS, then use a simplified DAG that starts from sensor → BQ load when pipeline is re-deployed.

- **Video Editing (Personal Project)** — Worked on creating a photo montage video for wife's CA exam success gift, inspired by Jersey movie railway station scene. Used DaVinci Resolve (free for Mac). Hands-on editing: clip duration adjustment, transitions, text overlays, zoom effects.

## Code Built

- `docs/fde-101-guide.html` — FDE 101 guide (614 lines, paper-aesthetic style, committed as `383d91c`)
- `imdb-data-pipeline/Dags/imdb_dag.py` — Refactored DAG (uncommitted, local changes only since Composer deleted)
- `imdb-data-pipeline/Extracts/extract_*_local.py` — Local extraction scripts (uncommitted)

## Errors Debugged

*(Continuation from May 20)*

- **Composer workers can't reach RapidAPI**: Confirmed that Cloud Composer's network isolation blocks outbound HTTPS to external APIs. The fix from yesterday (`timeout 120` + `execution_timeout`) handles the symptom (zombie tasks) but not the root cause (network). Resolution: run extraction locally, use GCS as intermediary.

- **DaVinci Resolve speed controls not working**: "Show speed controls" option was unclickable. No straightforward fix found — user worked around by adjusting clip duration individually.

## Interview Prep

- **FDE terminology mapping** — Prepared vocabulary for Forward Deployed Engineering interviews: production debugging, customer-facing engineering, P0 incident response, feature flags, canary deployments, rollback strategies.
- **IMDb pipeline STAR framing** — "How did you take data from API?" — discussion on how to frame manual data extraction as intentional architecture choice for interview answers. Pattern: local extraction → GCS → BQ is a valid separation of concerns.

## Tomorrow

- Resume the 56-day plan (Day 13+ topics)
- Re-deploy IMDB pipeline with the local-extraction architecture when needed
