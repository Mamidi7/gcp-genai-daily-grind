# Daily Log — 2026-05-16

## What We Covered

- **Day 8: Flask + Docker + Cloud Run Notebook Redesign** — The interview mastery notebook received a complete UI overhaul with tab-based navigation (Service/Infrastructure views), SVG architecture diagrams (request lifecycle + deployment pipeline), interactive playground with step-by-step animation, 10-question mastery quiz with animated bar results, copy-buttons on code blocks, dark mode toggle with localStorage persistence, scroll-reveal section animations, and section-by-section progress tracker. Content remains the same: Flask 3-endpoint API, gunicorn production config, Docker multi-stage build with layer caching, Cloud Run source deploy flow.

- **Opencode Go in Codex Setup Exploration** — Investigated how to configure opencode go as a provider inside OpenAI Codex CLI for using the opencode subscription plan as model/provider within Codex workflows.

## Code Built

- `56_DAY_PRACTICE/day8/day8-flask-cloud-run-notebook.html` — Redesigned (646 insertions, 716 deletions): tab system (Service/Infrastructure tabs), SVG node-based architecture diagrams with CSS variable color theming, interactive playground with 3 routes (GET /, /health, /info) and animated step-by-step simulation, 10-question quiz with Telegram-poll-style UI, card-based layout with hover lift shadows, progress tracker (8 sections with localStorage), copy-to-clipboard buttons on code blocks, dark/light mode toggle with sun/moon SVGs.

## Errors Debugged

- N/A — no new errors debugged today. The notebook redesign was a CSS/JS exercise building on existing content.

## Interview Prep

- **Flask + Docker + Cloud Run** — Existing interview content expanded with better presentation: STAR blocks, 30s/90s/3-min walkthrough still present in the Infrastructure tab. Mastery quiz reinforces 10 key gotchas (gunicorn requirement, PORT binding, 0.0.0.0 vs 127.0.0.1, layer caching, .dockerignore, CMD format).

## Other Sessions

- **Opencode Go + Codex** — TUI session exploring how to use opencode go subscription as a model/provider inside OpenAI Codex CLI. This is a tooling configuration topic, not directly GCP/learning related.

## Tomorrow

- Continue extending FastAPI-Gemini app (more endpoints, streaming support)
- Deploy Terraform solution to actual GCP project
- More GCP IAM/Cloud Storage console practice
