# Day 01 - 2026-03-21

## daily_topic
Interview signal system setup + SQL to Python bridge preparation.

## what_built
Created the `interview_signal` module with templates, tracker, and tool usage matrix.

## what_failed
Initial Gemini CLI run failed due auth mode set to API key with no key configured.

## fix_applied
Switched to Vertex mode for test run and pinned a stable model (`gemini-2.5-flash`) for successful execution.

## prevention
Use explicit auth mode and model selection in command runs; document defaults in notes before execution.

## interview_30s
I built a repeatable interview-signal system that turns daily coding into artifacts. During setup, I debugged a Gemini CLI auth failure, switched to Vertex mode, and stabilized execution by selecting an accessible model.

## interview_90s_star

Situation:
I had strong learning activity but inconsistent interview-ready evidence.
Task:
I needed a daily, low-overhead process that captures build + failure + fix for portfolio and interviews.
Action:
I created a 14-day interview-signal engine with strict templates for artifacts, debug journals, and STAR packs. While validating automation, I hit an auth-mode failure in Gemini CLI and fixed it by using Vertex auth and a supported model.
Result:
Now each day has a clear output contract, and I can turn technical work into measurable interview stories with failure-path evidence.

## interview_3min
The main challenge was not learning content but converting it into hiring signal. I introduced a daily artifact contract with nine required fields: topic, build result, failure, fix, prevention, and three answer depths. This removes ambiguity at end of day and ensures every session is measurable.

I also added a 14-day tracker tied to specific tools by purpose: API validation, debug confidence, portfolio visuals, and speaking practice. This avoids random tool use and keeps the time-box at 30 minutes.

During validation, the first CLI call failed because auth mode was pinned to API key without configured credentials. I tested with Vertex mode and a stable model (`gemini-2.5-flash`), got a successful response, and documented the failure path as interview evidence.

This gives me repeatable proof: not just that I can code, but that I can debug, recover, and explain tradeoffs under constraints.

## tools_used

- tool: roadmap.sh
- why: Weekly skill-gap planning baseline.
- artifact: Gap-map checklist.

- tool: httpie.io/app
- why: API contract validation on backend routes.
- artifact: Request/response capture template.

- tool: webhook.site
- why: Observe callback payloads for event debugging.
- artifact: Webhook payload evidence screenshot note.

## evidence_links

- code: `56_DAY_PRACTICE/interview_signal/`
- screenshot: `56_DAY_PRACTICE/interview_signal/portfolio_assets/`
- notes: `56_DAY_PRACTICE/interview_signal/interview_packs/`
